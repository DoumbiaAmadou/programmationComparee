;redcode-94
;assert     CORESIZE >= 1 && MAXLENGTH >= 7
;name       GOTO YOLO
;author     fontaine
            org     start

start       JMP #2
            JMP #12
            DAT #0, 0
            ADD #11, -1
            MOV -3, -2
            JMP #-6
